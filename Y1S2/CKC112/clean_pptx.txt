 Topic 8


Recursion

 Dr. Mohammad Ali Sarvghadimasarvghadi@usm.mymasarvghadi@usm.my for UOW1

--- Slide Break ---

              Topic 8RecursionRecursionmasarvghadi@usm.my for UOW2

--- Slide Break ---

Introduction to RecursionA function that calls itself is a recursive function.				void message()		{			cout << "This is a recursive function.\n";			message();		}masarvghadi@usm.my for UOW3

--- Slide Break ---

Introduction to Recursion (Cont.)A function that calls itself is a recursive function.				void message()		{			cout << "This is a recursive function.\n";			message();		}What is the problem with the function above? masarvghadi@usm.my for UOW4

--- Slide Break ---

Introduction to Recursion (Cont.)A function that calls itself is a recursive function.				void message()		{			cout << "This is a recursive function.\n";			message();		}What is the problem with the function above? It acts like an infinite loop which leads the program to crash due to stack overflow.masarvghadi@usm.my for UOW5

--- Slide Break ---

Stack MechanismThe computer’s memory for recursive functions operates like a vertical stack of dishes.It follows the Last-In, First-Out (LIFO) mechanism.The last item pushed onto the stack is the first item to be popped (removed).In recursive functions, each call pushes a new stack frame (containing the return address and local variables) onto the memory.Because computer memory is finite, pushing frames infinitely leads to a Stack Overflow, the point where the "pile" hits the ceiling and crashes.masarvghadi@usm.my for UOW6

--- Slide Break ---

Introduction to Recursion (Cont.)Solution		void message(int times)		{		   if (times > 0)		   {			cout << "This is a recursive function.\n";			message(times − 1);   recursive call		   }		}	  Control returns here from the recursive call, causing the function to return.  masarvghadi@usm.my for UOW7

--- Slide Break ---

Introduction to Recursion (Cont.)masarvghadi@usm.my for UOW8CodeOutputvoid message(int times){   cout << “Called with " << times << " in times.\n";   if (times > 0)   {      cout << “A recursive function.\n";      message(times − 1);   }   cout << “Returning with " << times;  }Function call: message(5);???

--- Slide Break ---

Introduction to Recursion (Cont.)masarvghadi@usm.my for UOW9CodeOutputvoid message(int times){   cout << “Called with " << times << " in times.\n";   if (times > 0)   {      cout << “A recursive function.\n";      message(times − 1);   }   cout << “Returning with " << times;  }Function call: message(5);Called with 5 in times.A recursive function.Called with 4 in times.A recursive function.Called with 3 in times.A recursive function.Called with 2 in times.A recursive function.Called with 1 in times.A recursive function.Called with 0 in times.Returning with 0Returning with 1Returning with 2Returning with 3Returning with 4Returning with 5

--- Slide Break ---

Solving Problems with Recursion – Binary SearchA problem can be solved with recursion if it can be broken down into successive smaller problems that are identical to the overall problem.masarvghadi@usm.my for UOW10Any problem that can be solved recursively can also be solved iteratively, using a loop.1stlast1stlast1stlast1stlast1stlastlast1st1stlastlast1st

--- Slide Break ---

Recursive Binary Search FunctionAlgorithmIf array[middle] equals the search value, then the value is found.Else, if array[middle] is less than the search value, perform a binary search on the upper half of the array.Else, if array[middle] is greater than the search value, perform a binary search on the lower half of the array.masarvghadi@usm.my for UOW11

--- Slide Break ---

Recursive Binary Search Function (Cont.)Codeint binarySearch(int array[], int first, int last, int value){   int middle;    if (first > last)      return −1;   middle = (first + last) / 2;   if (array[middle] == value)      return middle;   if (array[middle] < value)      return binarySearch(array, middle+1,last,value);   else      return binarySearch(array, first,middle-1,value);}masarvghadi@usm.my for UOW12

--- Slide Break ---

Iterative Binary Search FunctionCodeint binarySearch(int array[], int first, int last, int value){
   while (first <= last){
      int middle = (first + last) / 2;

      if (array[middle] == value)
         return middle;
      else if (array[middle] < value)
         first = middle + 1;
      else
         last = middle - 1;
   }

   return -1;
}masarvghadi@usm.my for UOW13

--- Slide Break ---

Solving Problems with Recursion (cont.)In fact, recursive algorithms are usually less efficient than iterative algorithms.Function calls impose overhead to the systemallocating memory for parameters and local variablesstoring the address of the program location where control returns after the function terminates.masarvghadi@usm.my for UOW14

--- Slide Break ---

Solving Problems with Recursion (cont.)Some repetitive problems, however, are more easily solved with recursion than with iteration.The programmer might be able to design a recursive algorithm faster.masarvghadi@usm.my for UOW15

--- Slide Break ---

Recursion RulesIf the problem can be solved now, without recursion, then the function solves it and returns.If the problem cannot be solved now, then the function reduces it to a smaller but similar problem and calls itself to solve the smaller problem.Base Case: identifying at least one case in which the problem can be solved without recursion.Recursive Case: determining a way to solve the problem in all other circumstances using recursion.The recursive case will continue until one of the base cases are met.masarvghadi@usm.my for UOW16What is/are the base case(s) in recursive binary search?

--- Slide Break ---

Recursion RulesIf the problem can be solved now, without recursion, then the function solves it and returns.If the problem cannot be solved now, then the function reduces it to a smaller but similar problem and calls itself to solve the smaller problem.Base Case: identifying at least one case in which the problem can be solved without recursion.Recursive Case: determining a way to solve the problem in all other circumstances using recursion.The recursive case will continue until one of the base cases are met.masarvghadi@usm.my for UOW17What is/are the base case(s) in recursive binary search?They are:array[middle] == value&first > last

--- Slide Break ---

Recursion TypesDirectA function calls itselfIndirectFunction A calls function B, and function B calls function A.Function A calls function B, function B calls function c . . .  and finally the last function calls function A.masarvghadi@usm.my for UOW18

--- Slide Break ---

FactorialThe factorial of a non-negative number can be defined by the following rules:	If n = 0 then n!  1	If n > 0 then n!  1  2  3  …  n	If n = 0 then factorial(n)  1	If n > 0 then factorial(n-1)  1  2  3  …  nfactorial(6) = 1  2  3  4  5  6	If n = 0 then factorial(n) = 1	If n > 0 then factorial(n) = n  factorial(n - 1)masarvghadi@usm.my for UOW19

--- Slide Break ---

Factorial (Cont.)int factorial(int n){   if (n == 0)      return 1;   else   return n * factorial(n − 1);}masarvghadi@usm.my for UOW20

--- Slide Break ---

Character Finderint numChars(char search, string str, int subscript){   if (subscript >= str.length())   {      return 0;   }   else if (str[subscript] == search){      return 1 + numChars(search, str, subscript+1);   }   else{      return numChars(search, str, subscript+1);  }}masarvghadi@usm.my for UOW21

--- Slide Break ---

Greatest Common Divisor (GCD)			masarvghadi@usm.my for UOW22AlgorithmCodegcd( x , y ) = y;	if y divides x evenlygcd( y , remainder of x / y );	otherwiseint gcd(int x, int y){   if (x % y == 0)      return y;   else      return gcd(y, x % y)}

--- Slide Break ---

masarvghadi@usm.my for UOW23

--- Slide Break ---

